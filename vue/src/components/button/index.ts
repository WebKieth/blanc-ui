import { Button, type ButtonProps, buttonProps } from './Button'
import {
  buttonStyle,
  buttonVariants,
  type ButtonSize,
  type ButtonVariant
} from '../../../../shared/components/button'

export {
  Button,
  buttonProps,
  type ButtonProps,
  buttonStyle,
  buttonVariants,
  type ButtonSize,
  type ButtonVariant
}